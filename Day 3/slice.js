//slice method is used to slice the string, we can set the start and end index of a string.

//Start Index and End Index.

let firstName = 'vijaygopal';
firstName = firstName.slice(0, 5); //0 is the start and 5 is end index of a string. Array indexing starts from 0 and the length of a string starts from 1.
console.log(firstName);

